# =============================================================
# AetherBlog · Git 属性
# =============================================================

# ---------- 追加型文档：用 union 合并策略消除必然冲突 ----------
#
# 问题：CHANGELOG.md 与设计系统 history.md 是「只追加」的日志文件，
# 每个分支都往同一个锚点（`## [Unreleased]` 正下方 / 文件末尾）插入自己的
# 条目。Git 看到两个分支在同一行位置写入不同内容就判定冲突 —— 但语义上
# 这从来不是冲突：两条条目都该保留。结果是每合入一个 PR，其余在途分支
# 都要为同一处手工解一次冲突。
#
# 解法：`merge=union` 是 Git 内置策略（非自定义 driver，无需任何额外配置），
# 冲突时不产生 <<<<<<< 标记，而是按「本方在前、对方在后」把两侧内容都保留。
#
# 已实测验证（沙盒复刻真实冲突形状）：
#   · 自动合并成功，退出码 0，零冲突标记
#   · 接缝处会少一个空行（该空行被 Git 判为上下文行），但 CommonMark 的
#     ATX 标题会自动闭合上方列表 —— 用 marked 渲染比对，产出的 HTML
#     与带空行版本逐字节相同，纯属源码观感，不影响任何渲染
#   · 发版把 `## [Unreleased]` 改名为版本号时也能正确合并（版本号标题与
#     新条目都保留）
#
# 唯一需要留意的场景：发版改名与「新增条目」并发时，新条目会落进刚发布的
# 版本章节而不是新的 Unreleased。这在 diff 中可见 —— 发版提交请单独做一次
# 并核对结果。
CHANGELOG.md merge=union
.claude/design-system/history.md merge=union
