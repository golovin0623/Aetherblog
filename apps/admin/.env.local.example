# Admin 后台环境变量配置
# 复制此文件为 .env.local 进行本地开发

# =============================================================================
# API 配置
# =============================================================================

# API 基础路径
# 开发模式: 使用相对路径，由 Vite proxy 代理到 localhost:8080
# 生产模式: 使用相对路径，由 Nginx 代理到 backend:8080
VITE_API_URL=/api

# =============================================================================
# 使用说明
# =============================================================================
#
# 开发模式 (./start.sh):
#   - 访问入口: http://localhost:5173
#   - API 调用: /api/* -> Vite proxy -> http://localhost:8080/api/*
#
# 开发网关模式 (./start.sh --gateway):
#   - 访问入口: http://localhost:7899/admin/
#   - API 调用: /api/* -> Nginx -> http://localhost:8080/api/*
#   - 注意: Vite base 为 '/'，网关会重写路径
#
# 生产模式 (Docker):
#   - 访问入口: http://gateway:7899/admin/
#   - API 调用: /api/* -> Nginx -> http://backend:8080/api/*
