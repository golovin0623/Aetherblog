# SECURITY (VULN-074): exact pins for reproducibility and supply-chain auditing.
# Floating `>=` specifiers let a single upstream typosquat / mis-release land
# silently on the next `pip install`. Dependabot / Renovate should own upgrades,
# not the build machine.
#
# Rule of thumb: bump these in a dedicated PR, attach pip-audit / gitleaks run
# output, and only merge when pnpm audit / govulncheck / trivy are all clean.
fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.13.0
pydantic-settings==2.11.0
eval-type-backport==0.3.1
python-dotenv==1.2.1
redis==7.0.1
asyncpg==0.31.0
PyJWT==2.12.1
httpx==0.28.1
litellm==1.83.0
pgvector==0.4.2
cryptography==46.0.7
sqlalchemy==2.0.49
# KB 文档解析（Phase 2）—— 仅在 kb_indexer.py 中按需 import，未装时退化失败。
pypdf==5.0.1
python-docx==1.1.2
trafilatura==2.0.0
