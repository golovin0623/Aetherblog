# Blog 前台环境变量配置
# 复制此文件为 .env.local 用于本地开发：
#     cp apps/blog/.env.local.example apps/blog/.env.local
# 或直接运行 ./start.sh --gateway，脚本会在缺失时自动 cp。

# =============================================================================
# 管理后台跳转入口
# =============================================================================
#
# blog 顶部右侧"齿轮"图标和移动端抽屉里的"管理后台"按钮依赖此变量。
# 未配置时按钮会以"管理后台未配置"的灰态显示，无法点击。
#
# 取值规则（见 apps/blog/app/lib/adminUrl.ts）：
#   - 必须以 http:// / https:// 开头，或以 / 开头作为相对路径
#   - 网关模式（./start.sh --gateway / 生产）：填 /admin/ 即可由 Nginx 路由
#   - 直连模式（./start.sh 无网关）：填 http://localhost:5173/ 直连 Vite admin
#   - 跨域生产部署：填 https://admin.your-domain.com/
NEXT_PUBLIC_ADMIN_URL=/admin/

# =============================================================================
# 后端 API 地址
# =============================================================================
#
# 不设时 server 端默认 http://localhost:8080，client 端走相对 /api 由 Nginx 代理。
# 通常本机开发不需要设置；只在跨域分离部署时才填。
#
# NEXT_PUBLIC_API_URL=
# API_URL=                   # 仅 server 端读取，优先级高于 NEXT_PUBLIC_API_URL

# =============================================================================
# 使用说明
# =============================================================================
#
# 开发模式 (./start.sh)：
#   - 访问入口: http://localhost:3000
#   - 管理后台跳转: NEXT_PUBLIC_ADMIN_URL=/admin/ 走相对路径，由当前 origin 拼接
#     → 直连模式下点击会跳到 http://localhost:3000/admin/，但 Vite admin 在 5173
#     → 直连模式建议改为 NEXT_PUBLIC_ADMIN_URL=http://localhost:5173/
#
# 开发网关模式 (./start.sh --gateway)：
#   - 访问入口: http://localhost:7899
#   - 管理后台跳转: /admin/ → Nginx 路由到 admin
#   - 默认值即为正确配置
#
# 生产模式 (Docker)：
#   - 访问入口: http://gateway:7899
#   - 同网关模式，默认值即可
