-- 000036 是一个纯修复迁移: 它把损坏的 post_embeddings 重建成 000034 定义的
-- 版本化 schema. 回滚 000036 并不意味着要退回到 000001 的 chunk 表结构
-- (那是个历史错误, 从未承载生产数据); 任何真正要废弃版本化 post_embeddings
-- 的操作都应该走 000034 的 down. 所以这里 no-op.
SELECT 1;
