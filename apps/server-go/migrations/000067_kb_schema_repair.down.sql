-- 000067 down: no-op。
--
-- 000067 是幂等「修复」迁移，只在缺失时补齐 KB schema；它不拥有这些表的
-- 生命周期。KB 表的真正 teardown 归 000058/000059/000060/000061 的 down 负责。
-- 若在此 DROP TABLE，会让 `migrate down 1`（仅回退 67）误删整套知识库数据 ——
-- 与"只回退一步"的语义严重不符。故 down 显式留空。
SELECT 1;
