---
skill: deploy
description: Build and deploy new Docker image versions to Docker Hub
---

# Deploy - 构建发布新版本

此命令用于构建 Docker 镜像并推送到 Docker Hub，同时生成版本发布总结。

## 执行流程

### 步骤 1: 检查当前最新版本

执行以下命令查询 Docker Hub 上的最新版本：

```bash
curl -s "https://hub.docker.com/v2/repositories/golovin0623/aetherblog-backend/tags/?page_size=5" 2>/dev/null | grep -o '"name":"v[^"]*"' | head -3
```

根据返回结果确定新版本号（当前最新版本 +1）。

版本号规则：
- 小功能修复：如 v1.1.3 -> v1.1.4
- 大功能新增：如 v1.1.3 -> v1.2.0
- 大版本迭代：如 v1.1.3 -> v2.0.0

### 步骤 2: 确认 Git 状态

执行以下命令检查未提交的更改：

```bash
git status --short
```

如果有未提交的更改：
1. 检查所有已修改的代码
2. 生成待提交的代码改动摘要
3. 执行 `git add . && git commit -m "your message"`

### 步骤 3: [关键!] 用户确认

**STOP!** 在此步骤必须暂停，等待用户确认。

向用户汇报以下信息并请求批准：

```
📦 部署确认
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🏷️  当前最新版本: [从步骤 1 获取]
🚀 计划发布版本: [建议版本号]

📝 包含的变更:
[列出 commit 记录摘要]

🔧 涉及模块:
[列出本次改动涉及的模块]

📦 构建计划:
[说明将构建哪些镜像: backend/blog/admin/全部]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请确认是否继续部署？(回复 "确认"、"批准" 或 "/approve")
```

**仅当用户明确回复 "确认"、"批准" 或 "/approve" 时，才继续执行步骤 4。**

### 步骤 4: 构建并推送 Docker 镜像

根据涉及的模块决定构建策略：

**全量构建（所有模块都有变更）：**
```bash
./docker-build.sh --push --version VERSION
```

**部分构建（仅特定模块）：**
```bash
# 仅后端
./docker-build.sh --only backend --push --version VERSION

# 仅博客前端
./docker-build.sh --only blog --push --version VERSION

# 仅管理后台
./docker-build.sh --only admin --push --version VERSION
```

**网络不稳定时使用串行构建：**
```bash
./docker-build.sh --push --sequential --version VERSION
```

此命令会：
- 并行构建 3 个镜像 (backend, blog, admin)
- 支持多平台 (linux/amd64, linux/arm64)
- 自动推送到 Docker Hub

### 步骤 5: 生成版本总结

查看自上次发布以来的所有提交：

```bash
git log --oneline origin/main..HEAD
```

根据提交记录生成版本总结，格式如下：

```markdown
## vX.X.X 版本更新

### 🐛 Bug Fixes
- [修复内容1]
- [修复内容2]

### ✨ Features
- [新功能1]
- [新功能2]

### 🔧 Improvements
- [优化内容1]
- [优化内容2]

### 📦 Docker Images
- golovin0623/aetherblog-backend:vX.X.X
- golovin0623/aetherblog-blog:vX.X.X
- golovin0623/aetherblog-admin:vX.X.X
```

### 步骤 6: 提供服务器部署命令

构建完成后，向用户提供服务器部署命令：

```bash
# 在服务器上执行以下命令
export DOCKER_REGISTRY=golovin0623
export VERSION=vX.X.X  # 替换为实际版本号
docker-compose -f docker-compose.prod.yml pull
docker-compose -f docker-compose.prod.yml up -d

# 查看日志
docker-compose -f docker-compose.prod.yml logs -f
```

## 快速命令参考

| 命令 | 说明 |
|:-----|:-----|
| `./docker-build.sh --push --version vX.X.X` | 构建并推送指定版本 |
| `./docker-build.sh --only backend --push --version vX.X.X` | 只构建后端 |
| `./docker-build.sh --only blog --push --version vX.X.X` | 只构建博客前端 |
| `./docker-build.sh --only admin --push --version vX.X.X` | 只构建管理后台 |
| `./docker-build.sh --sequential --push --version vX.X.X` | 串行构建（网络不稳定时使用） |

## 注意事项

- 版本号格式：`vX.Y.Z`（如 v1.1.3）
- 构建时间约 2-3 分钟（取决于网络和缓存）
- 如遇构建失败，检查 Docker 登录状态和网络连接
- 确保 Docker Desktop 正在运行
- 确保已登录 Docker Hub (`docker login`)

## 前置条件检查

在开始部署前，自动检查以下条件：

1. Docker Desktop 是否运行：`docker info > /dev/null 2>&1`
2. Docker Hub 登录状态：`docker info | grep Username`
3. Git 工作区状态：`git status --short`

如果任何前置条件不满足，提示用户解决后再继续。
