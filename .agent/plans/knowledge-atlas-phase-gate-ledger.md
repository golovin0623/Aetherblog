# Knowledge Atlas Phase Gate Ledger

> Date: 2026-05-31
> Worktree: `codex/knowledge-atlas-landing`
> Source plan: `.agent/plans/knowledge-atlas-gap-analysis-iteration-review.md`

## Gate Policy

This ledger separates "MVP route exists" from "product phase gate passed". A phase is not marked passed unless the code, UI, tests, and product red lines have current evidence.

## Current Gate Status

| Gate | Status | Current evidence | Remaining work |
| --- | --- | --- | --- |
| P0-01 real Atlas dashboard | Implemented with authenticated smoke evidence | `/atlas` now loads health, KP, pending suggestions, live graph health metrics, recent KP, and quick links; `run-release-smoke.mjs` reached `/admin/atlas` through gateway and matched `Aether Atlas` with warm LCP `956ms` | Re-run on production deployment before release |
| P0-02 subpage navigation | Implemented with authenticated smoke evidence | Dashboard quick links plus Sidebar links for `/atlas`, `/atlas/search`, `/atlas/kps`, `/atlas/graph`, `/atlas/suggestions`; `/atlas/kps` and `/atlas/search` routes added; Note edit header has an Atlas action that saves dirty content before opening `/atlas/reader/note/:noteId`; release smoke opened `/admin/atlas/reader/note/15`, `/admin/atlas/kps`, `/admin/atlas/kp/40`, `/admin/atlas/graph`, and `/admin/atlas/suggestions` | Manual click-path recording can still be added, but route-level gateway smoke now exists |
| P0-03 Multi-user Gate execution | Implemented with non-admin gateway smoke evidence | Atlas routes now use RBAC read/write/admin permissions instead of legacy admin-only grouping; `AtlasScopeMiddleware` resolves `content.atlas.admin`; list/get/graph/relation/suggestion/carrier/annotation paths enforce author/owner scope; Dashboard/KP/Graph/Suggestions expose `all`/`mine` scope controls; unit tests cover non-admin author switching and admin scope modes; `run-multiuser-smoke.mjs` temporarily grants `AUTHOR` Atlas read/write, logs in two non-admin authors through gateway, creates author-scoped Reader notes `16`/`17` and KPs `43`/`44`, opens each author's Markdown Reader page, proves author B gets `403` on author A's Reader source and cannot see/switch to author A KP data, proves admin `scope=all`/`authorId` visibility, and restores `AUTHOR` Atlas permissions after the run | Re-run on production deployment before exposing Atlas permissions broadly |
| P0-04 graph scoped edges | Implemented | `RelationRepo.ListForNodeIDs` filters `from_kp_id` and `to_kp_id` with the returned node set and optional author scope; unit tests cover empty, node-scoped, and author-scoped cases | Local graph depth and graph inspector remain Sprint 1/3 work |
| P0-05 suggestion source binding | Implemented for KP suggestions | `AISuggestionService.Create` rejects `kind=kp` without `carrierId` or `annotationId`; production demo button removed from Suggestions page | Real AI extraction still P2; relation suggestion evidence policy should be revisited with relation extraction |
| P0-06 ignored/pending suggestion dedupe | Implemented | `fingerprint` column/index migration; service checks ignored fingerprint and returns existing pending duplicate | Backfill fingerprint for old pending suggestions is not included |
| P0-07 relation evidence API | Implemented | Create relation accepts `evidenceAnnotationIds`; `POST/GET/DELETE /relations/:id/evidence` added; AI relation accept links suggestion annotation as evidence; KP detail relation form can attach current-KP evidence and relation cards show evidence quotes | Dedicated relation evidence delete UI is still future polish |
| P0-08 baseline tests/redline evidence | Implemented for Sprint 0 scope with current admin and non-admin smoke | `go test ./internal/knowledge/...`; `go test ./...`; `pnpm --filter @aetherblog/admin typecheck`; `pnpm --filter @aetherblog/admin build`; Atlas native select/spinner/demo redline grep is clean; R1/R2/R3/R4/R5 gate scripts now provide repeatable command evidence; R1 real-PDF corpus gate passed 3 representative PDFs / 9 original pages / 12 variants / 45 anchorable checks; R2 live non-admin user dataset gate seeded 5 KPs / 10 relations through gateway API and live graph health passed density/evidence/orphan thresholds; R3 live gate now preflights Atlas task/routing/credential readiness, rejects heuristic fallback, and passed with explicit live model `gemini-3.1-flash-lite-preview` (`kp_accept=100%`, `relation_accept=50%`, schema/grounding/non-stub/token coverage `100%`); authenticated admin gateway smoke passed 15/15 checks; authenticated multi-user gateway smoke passed 17/17 checks on 2026-05-31, including non-admin Markdown Reader page sessions | Live production metrics remain separate release evidence |
| P0-11 source URI uniqueness | Implemented at schema/repo level | Migration `000070` replaces global `UNIQUE(source_uri)` with live per-owner expression index; carrier source lookup/upsert is owner-scoped | Rollback can fail if per-owner duplicates are created before reverting, because the down migration restores global uniqueness |
| P0-12 phase gate ledger | Implemented | This file records evidence and remaining work | Must be updated at each Atlas sprint exit |
| P2-10 data integrity hardening | Implemented | Migration `000070` adds `proposed_kp_type` CHECK and `carrier_version_id` index; service rejects invalid `proposedKpType` | Existing invalid historical suggestions are not corrected |
| P1-01 Reader inline highlight | Implemented with authenticated PDF, real-PDF corpus, and non-admin Reader smoke | Markdown Reader overlays anchored/soft annotation ranges into rendered Markdown with state-colored `<mark>` nodes and keeps orphan annotations in the side list only; `scripts/atlas/anchoring-recall.mjs --min-recall 0.9` proves 23/23 Markdown version-migration anchorable cases recalled and 1/1 deliberate orphan retained; `scripts/atlas/pdf-anchoring-recall.mjs --min-recall 0.9` proves 23/23 PDF text-layer version-migration anchorable cases recalled and 1/1 deliberate orphan retained; `scripts/atlas/pdf-real-corpus-gate.mjs --min-recall 0.9` builds 3 representative multi-page PDFs, extracts them through ai-service's `pypdf` dependency, and passes 45/45 anchorable checks plus 3/3 deliberate orphan checks; PDF media carrier ingest now calls ai-service `POST /v1/atlas/pdf/extract`, persists extracted rootText in `atlas_carrier_text_layers`, stores the immutable `atlas-text-layer://pdf/...` URI in carrier versions, and PDF Reader now reads the persisted text layer, creates TextQuote/TextPosition/PageRect selectors, overlays page rects, and jumps back to `page+rect` from annotation evidence links; release smoke uploaded a generated one-page PDF, created carrier `46`, verified text layer page count `1`, and opened `/admin/atlas/reader/pdf/46`; multi-user smoke created non-admin Reader notes `16`/`17`, opened `/admin/atlas/reader/note/16` and `/admin/atlas/reader/note/17`, and verified cross-user Reader source access returns `403` | Re-run on production/user PDF samples before release |
| P1-02 annotation to KP | Implemented baseline | Markdown Reader annotation cards now open an editable KP draft for title/body/type/status/confidence/evidence role, create the KP, and link the annotation as the selected evidence role | Multi-annotation batching and browser smoke remain future improvements |
| P1-03 annotation to AI suggestion | Implemented baseline | Reader annotation cards can call `POST /atlas/annotations/:id/suggestions`; server-go validates annotation scope, calls ai-service `/v1/atlas/claims/extract` through `X-Internal-Service`, and writes pending KP suggestions into Inbox | Async batch extraction, progress reporting, and model-quality iteration remain P2 work |
| P1-04 KP list | Implemented baseline | `/atlas/kps` provides keyword/type/status/provenance/evidence filters, scope switching, health summary, and quick links to KP detail | Bulk actions remain Sprint 1+ polish |
| P1-05 KP edit/archive/delete | Implemented with authenticated lifecycle smoke evidence | KP detail exposes edit modal for title/body/type/status/confidence, archive/restore, and delete confirmation wired to existing APIs with toast/error states; release smoke created lifecycle KP `42`, clicked archive and verified `archived=true/status=archived`, clicked restore and verified `archived=false/status=growing`, then confirmed delete and verified `GET /knowledge-points/42` returned `404` | Production deployment rerun remains release evidence |
| P1-06 relation creation guide | Implemented baseline | KP detail relation form uses styled `Select`, relation explanations, target KP selection, strength, rationale/body, and current-KP evidence attachment; relation list shows rationale and evidence quotes | Target KP search is still limited to the loaded KP list, not full async search |
| P1-07 global search | Implemented semantic baseline | Dashboard search deep-links into `/atlas/search`; `GET /atlas/search` aggregates KP title/body, Annotation body/selectors, and Carrier title/source/author with Atlas scope enforcement; `/atlas/search?semantic=true` calls ai-service Atlas semantic recall, hydrates scoped KP rows, ranks semantic hits ahead of keyword-only hits, and exposes `searchScore` / `searchSource` plus degradation status; KP list still supports backend keyword/type/status/provenance/evidence filters; Graph page can keyword-locate KP and records graph search telemetry | Full GraphRAG/community/global ranking remains later work; production recall completeness still depends on running the KP/note embedding backfill |
| P1-08 control and skeleton redline | Implemented for Atlas pages touched in this sprint | Atlas dashboard, KP list/detail, graph, suggestions, and markdown reader use shared `Select`/skeleton states; redline grep has no native select/spinner/demo matches in Atlas pages | Broader admin design-system automation is not available in this worktree |
| P1-09 AetherHub Atlas scope | Implemented semantic baseline | AetherHub can select the current user's Atlas KP and now always sends `atlasScope` with `semanticRecall=true`; selected KP ids seed the local graph when present, while empty `kpIds` lets arbitrary questions use query-based semantic KP/note recall; ai-service merges selected KPs, semantic hits, recursive relation-neighborhood rows, and evidence into the Agent system context; answer completion records KP/Evidence/Annotation/Note citation counts | Public knowledge-map surfacing and full GraphRAG/community/global query remain later work |
| P1-10 user guide | Implemented baseline | `docs/atlas-user-guide.md` documents note -> annotation -> KP -> relation -> graph workflow, async KP/note embedding indexing, empty-scope AetherHub semantic recall, and the selected-scope Atlas handoff | Full GraphRAG/community/global query remains later work |
| P1-12 analytics events | Implemented baseline | Atlas writes activity events for annotation creation, annotation->KP, suggestion accept/reject, Atlas search, graph search, and AetherHub Atlas answer citation counts (`atlas.*` event types under `system` category); `/atlas/events` is restricted to known telemetry event types | Metrics still need real user data and dashboard aggregation |
| R2-01 relation health gate | Implemented with fixed-corpus, JSON/DB evaluator, and live non-admin user dataset proof | `scripts/atlas/relation-health-gate.mjs` measures active KP count, typed relation density, KP evidence coverage, relation evidence-or-rationale coverage, orphan ratio, invalid edge references, and relation type validity; fixed corpus passes density `2.00`, KP evidence `100%`, relation evidence/rationale `100%`; `GET /atlas/graph/health` computes the same core density/evidence/orphan/hub metrics against the scoped live DB dataset and the dashboard reads it; `scripts/atlas/run-relation-health-live-gate.mjs` temporarily grants `AUTHOR` Atlas read/write, creates non-admin user `15`, seeds note `18`, carrier `53`, annotations `16`-`20`, KPs `45`-`49`, relations `13`-`22`, reads `/atlas/graph/health?scope=mine`, and verifies active KPs `5`, relations `10`, density `2.00`, KP evidence `100%`, relation evidence `100%`, orphan `0%`; `AUTHOR` Atlas permissions restored to `[]` after the run | Re-run on production deployment before release |
| G1-01 local graph | Implemented baseline | KP detail includes a depth 1/2/3 local graph view loaded from scoped relation APIs and updates when depth changes | It remains an inline local-neighborhood SVG; advanced shared graph work belongs to the global graph surface |
| G1-02 zoom/pan/minimap | Implemented baseline | `/atlas/graph` now uses an SVG viewport transform with toolbar zoom controls, wheel zoom, drag pan, reset view, a minimap with viewport rectangle, and bounded pan/zoom so the graph cannot be dragged fully out of view | A future thousand-node graph may still require Sigma/Cytoscape or progressive loading |
| G1-03 graph inspector | Implemented baseline | `/atlas/graph` now lets users click a KP or relation to inspect metadata, evidence counts, degree/in/out summary, relation endpoints, rationale/body preview, and navigation actions without leaving the graph; selected nodes/edges are highlighted in the SVG | Richer evidence quote previews remain later graph UX work |
| G1-04 graph filters | Implemented baseline | `/atlas/graph` now filters by keyword, scope, KP type, relation type, updated-time window, provenance, confidence threshold, KP/relation evidence health, orphan KP, and hub KP; `GET /atlas/graph` returns scoped KP/relation evidence-count maps used by the evidence-health filter and inspector; graph filter presets can save/apply/delete the current filter set in scoped localStorage | Server-side/shared filter presets remain later collaboration polish |
| G1-05 layout persistence | Implemented baseline | `/atlas/graph` can save visible node coordinates and current viewport to scoped localStorage, restore them after refresh, and reset the saved layout | Topic-board/whiteboard-level persistent layouts remain A3-06 work |
| G1-06 graph health metrics | Implemented with live API/dashboard and seeded user threshold report | `GET /atlas/graph/health` returns active KP count, relation count/density, orphan KP count/ratio, KP evidence coverage, relation evidence-or-rationale coverage, missing evidence counts, AI KP count, and top hubs under the same Atlas author scope as `/graph`; `/atlas` dashboard no longer derives health from a limited graph payload; R2 live gate captured a scoped non-admin user dataset and threshold report through the same API | Re-run on production deployment before release |
| P2-01 structured claim extraction | Implemented baseline with task-type seed and live model evidence | ai-service Atlas claim extraction now uses a LlmRouter structured JSON wrapper when Atlas task routing or an explicit model is available; pydantic validates candidate schema and retries once before heuristic fallback; migration `000072` seeds `atlas_claims` / `atlas_relations` task types and inherits system chat routing when available; unit tests cover invalid JSON repair; the R3 live gate generated non-stub KP suggestions through server-go -> ai-service using `gemini-3.1-flash-lite-preview` | Configure production default Atlas routing credentials before release so operators do not need the explicit model override used for this local proof |
| P2-02 batch carrier extraction | Implemented synchronous bounded baseline | `POST /atlas/carriers/:id/suggestions` validates carrier scope, reads Markdown note root text, persisted PDF text layer, or blog post source text, calls ai-service `/v1/atlas/claims/extract` through `X-Internal-Service`, and writes carrier-bound pending KP suggestions into Inbox; Markdown/PDF Reader headers expose `全文 AI 建议`; AI Writing workspace exposes a post Atlas suggestion action; handler tests cover route registration, request default/cap logic, Markdown/blog post source parsing, and budget payload propagation | Full async background job and progress reporting remain follow-up P2-02 work |
| P2-08 carrier extraction cost budget | Implemented Reader preflight baseline | ai-service exposes `/v1/atlas/claims/preview` to estimate input/output tokens and cost from `usage_logger` token estimation plus LlmRouter price context; server-go exposes `POST /atlas/carriers/:id/suggestions/preview`, forwards `maxCostUsd` to preview/extract, and maps cost-budget exhaustion to a 400 response; Markdown/PDF Reader now preflight the run, warn when pricing is missing, and block generation when estimated cost exceeds the per-run threshold | Persistent user-configurable budgets, usage dashboards, and async batch-job cost rollups remain later work |
| M2-02 blog post carrier | Implemented baseline | `POST /atlas/carriers/post` wraps accessible posts as `blog_post` carriers with `posts://{id}` source URIs; `BlogPostCarrierService` enforces owner/admin scope and builds stable title/summary/body root text; migration `000075` extends the real `atlas_carriers.type` CHECK constraint to include `blog_post`; carrier-level preview/extract now supports blog posts; AI Writing workspace has an Atlas action that creates the carrier, previews cost, and generates pending KP suggestions into Inbox | A dedicated blog-post Reader/highlight surface, public blog knowledge panels, and web/video/audio carriers remain later multimodal/publishing work |
| M2-03 web clip carrier | Implemented Reader baseline | `POST /atlas/carriers/web` accepts a caller-supplied webpage snapshot (`sourceUrl`, title, Markdown body, optional author/language), normalizes http(s) URLs without fragments, writes owner-scoped `web` carriers, and persists a one-page `atlas_carrier_text_layers` rootText artifact; `/atlas` now has a Web snapshot entry that opens `/atlas/reader/web/:carrierId`; Web Reader reads the saved text layer, creates TextQuote/TextPosition/Css/Fragment selectors, lists/deletes annotations, and supports annotation-level plus full-text AI suggestions with the existing preflight budget guard; release smoke coverage now includes a Web Reader surface | Browser/readability automatic capture, richer Web quote metadata extraction, and public web citation polish remain later multimodal work |
| A3-02 writing assistant Atlas references | Implemented baseline | AI Writing workspace has an Atlas reference panel that builds a bounded query from the current title/summary/body, calls scoped semantic `/atlas/search`, lists related KPs with scores, and inserts `/admin/atlas/kp/:id` Markdown references into the editor; desktop and mobile Playwright smokes cover retrieval and insertion | Automatic evidence-quote insertion, public citation formatting, and richer writing-agent synthesis remain later polish |
| P2-03 relation suggestion | Implemented baseline | KP detail can request a relation suggestion for current KP + target KP; server-go calls ai-service `/v1/atlas/relations/suggest`, validates scope, and writes a pending relation suggestion to Inbox; ai-service validates relation type against the 9-type enum and retries once | Relation evidence attachment and quality metrics remain open |
| P2-04 KP embedding pipeline | Implemented baseline with backfill command | Migration `000073` binds KP embeddings to `search_profiles` via `embedding_profile_id` / `embedding_model_id` / `embedding_indexed_at` and adds 768/1024/1536/3072 partial HNSW buckets; ai-service `atlas_recall.py` writes KP title/body/evidence embeddings through `llm_router.embed` with strict active-profile model id; server-go triggers async KP indexing after create/update/evidence link/suggestion accept; `POST /v1/atlas/knowledge-points/index-batch` and `scripts/atlas/reindex-embeddings.mjs --kind knowledge-points` backfill missing/stale historical KPs; tests cover profile-bound indexing, batch candidate selection, and the Go internal index client | Production deployment must run the backfill command and archive its output before claiming production recall completeness |
| P2-05 Atlas recall | Implemented semantic baseline | ai-service `atlas_recall.py` merges selected KPs, carrier-derived KPs, active-profile pgvector semantic KP hits, Markdown carrier note chunk hits, recursive CTE relation neighborhood, and evidence; `agent.py` uses the last user message as recall query; AetherHub sends `semanticRecall`/`semanticLimit` for both selected KP and empty-scope automatic recall; `/v1/atlas/search/semantic` reuses the same active-profile recall path for search-page rerank; tests cover halfvec recall, graph-neighborhood fusion, note carrier chunk recall, rendering, Agent query handoff, and the semantic search route contract | Full GraphRAG/community/global query remains later work |
| P2-11 D2 note embedding closure | Implemented baseline with backfill command | Migration `000074` adds `embedding_dim` / `model_id` / `token_count`, a `(note_id, profile_id, chunk_index)` unique index, and 768/1024/1536/3072 partial HNSW buckets to `note_embeddings`; ai-service `NoteIndexerService` reuses the active search profile and strict embedding model to chunk/write note embeddings; `/v1/notes/{note_id}/index` is available behind admin/internal auth; server-go triggers async note indexing after create/update/duplicate and title/summary property edits; `POST /v1/notes/index-batch` and `scripts/atlas/reindex-embeddings.mjs --kind notes` backfill missing/stale historical notes; Atlas recall reuses Markdown carrier `notes://{id}` chunks as `[Note #id chunk n]` context | Production deployment must run the backfill command and archive its output before claiming production recall completeness; broader carrier-wide embedding unification remains a later architectural decision |
| P2-07 AI quality gate | Passed fixed-corpus/JSON/DB evaluator plus live non-stub model gate | `scripts/atlas/ai-quality-gate.mjs` measures final KP/relation suggestion acceptance, schema validity, and grounding; fixed corpus passes KP accept `62.50%`, relation accept `50.00%`, schema `100%`, grounding `100%`; `scripts/atlas/run-ai-quality-live-gate.mjs` seeds an isolated non-admin source dataset, requires Atlas AI task/routing readiness, rejects `atlas-stub/heuristic-v1`, and on 2026-05-31 passed through gateway/server-go/ai-service with explicit model `gemini-3.1-flash-lite-preview`: final suggestions `4`, KP accept `100%`, relation accept `50%`, schema valid `100%`, grounded `100%`, non-stub `100%`, token coverage `100%`; the gate also restored temporary `AUTHOR` Atlas permissions after the run | Broader prompt/model A/B comparison and real production user telemetry remain post-gate optimization evidence, not blockers for this scoped PR gate |
| R4-01 performance budget gate | Passed current build/runtime budget | `scripts/atlas/performance-budget-gate.mjs --lcp-ms 956 --graph-fps 59.7` passes with Atlas JS `242.9KiB`, CSS `0B`, largest JS `1.38MiB`, LCP `956ms`, and graph FPS `59.7`; the smoke runner records warm-route LCP after the dev gateway has compiled the Atlas dashboard chunk | Re-run against production deployment assets before release |
| R5-01 release smoke report gate | Passed current authenticated admin and multi-user gateway smoke | `scripts/atlas/run-release-smoke.mjs` seeds a note, Markdown carrier, annotation, KP pair, lifecycle KP, relation, suggestion, uploaded PDF, and PDF carrier, then visits Admin, Atlas, Reader note/PDF, KP list/detail, Graph, Suggestions, Notes, KnowledgeBase, AetherHub, and Blog through `http://localhost:7899`; it also browser-clicks KP archive/restore/delete; `release-smoke-gate --input output/playwright/atlas-release-smoke-report.json` passed 15/15 checks; `run-multiuser-smoke.mjs` plus `multiuser-smoke-gate.mjs` passed 17/17 non-admin/admin scope checks through the same gateway, including non-admin Reader sessions and cross-user Reader source denial | Re-run both smoke reports on production deployment before release |
| P3-06 search page | Implemented semantic baseline | `/atlas/search` provides a scoped search UI over KP, Annotation, and Carrier results; the default semantic rerank toggle sends `semantic=true`, displays semantic/keyword source chips and similarity score chips on KP results, and shows a degradation banner when ai-service recall is unavailable; backend escapes LIKE wildcards, caps per-kind results, and keeps keyword fallback intact | GraphRAG/community/global search ranking remains P3-05/P3-07 work |

## Verification Commands

| Command | Result |
| --- | --- |
| `go test ./internal/knowledge/repository -run TestRelationRepoGraphHealthComputesLiveMetrics -count=1` from `apps/server-go` | Passed after adding live graph health aggregation coverage |
| `go test ./internal/knowledge/repository -run TestCarrierRepoFindTextLayerByCarrierAndHash -count=1` from `apps/server-go` | Passed after adding the current-hash PDF text-layer read path |
| `go test ./internal/knowledge/...` from `apps/server-go` | Passed after adding `GET /atlas/carriers/:id/text-layer` and PDF Reader support |
| `go test ./...` from `apps/server-go` | Passed after PDF text-layer read/jump-back support |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after PDF Reader route, service types, and media/KP/search entrypoints |
| `pnpm --filter @aetherblog/admin build` | Passed after PDF Reader route; Vite emitted existing large-chunk warnings only |
| `git diff --check` | Passed after PDF text-layer read/jump-back support |
| `go test ./internal/knowledge/...` from `apps/server-go` | Passed after adding `GET /atlas/graph/health` |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after dashboard graph health API integration |
| `go test ./...` from `apps/server-go` | Passed after live graph health metrics |
| `pnpm --filter @aetherblog/admin build` | Passed after dashboard graph health API integration; Vite emitted existing large-chunk warnings only |
| `node scripts/atlas/relation-health-gate.mjs` | Passed fixed corpus after live graph health API; density `2.00`, KP evidence `100%`, relation evidence/rationale `100%` |
| `go test ./internal/knowledge/...` from `apps/server-go` | Passed after Atlas search repository coverage |
| `go test ./internal/knowledge/repository ./internal/knowledge/service ./internal/knowledge/handler` from `apps/server-go` | Passed after P1 relation evidence role upsert and Atlas telemetry handler tests |
| `go test ./...` from `apps/server-go` | Passed |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after `pnpm install --offline --ignore-scripts` initialized the new worktree node_modules |
| `pnpm --filter @aetherblog/admin build` | Passed; Vite emitted existing large-chunk warnings only |
| `rg -n "<select\|Loader2\|animate-spin\|P3-DEMO\|Phase 0 占位\|严禁" apps/admin/src/pages/atlas apps/admin/src/services/atlasService.ts apps/admin/src/components/layout/Sidebar.tsx` | No matches |
| `rg -n "<select\|P3-DEMO\|Phase 0 占位\|严禁" apps/admin/src/pages/notes/CreateNotePage.tsx apps/admin/src/pages/atlas apps/admin/src/services/atlasService.ts apps/admin/src/components/layout/Sidebar.tsx` | No matches after replacing the Note folder dropdown with shared `Select` |
| `PYTHONPATH=. uv run pytest tests/test_atlas_routes.py -q --no-cov` from `apps/ai-service` | Passed for structured Atlas extraction/relation retry tests |
| `PYTHONPATH=. uv run pytest tests/test_atlas_recall.py tests/test_atlas_routes.py tests/test_agent_routes.py -q --no-cov` from `apps/ai-service` | Passed after adding profile-bound Atlas KP embedding, semantic recall, recursive neighborhood rendering, internal KP index route, and Agent query handoff tests |
| `PYTHONPATH=. uv run pytest tests/test_note_indexer.py tests/test_atlas_recall.py -q --no-cov` from `apps/ai-service` | Passed after adding D2 note embedding worker tests and Markdown carrier note chunk recall |
| `PYTHONPATH=. uv run pytest tests/test_atlas_routes.py tests/test_notes_routes.py -q --no-cov` from `apps/ai-service` | Passed after adding historical KP/note embedding backfill endpoints |
| `PYTHONPATH=. uv run python -m py_compile app/api/routes/notes.py app/services/note_indexer.py app/services/atlas_recall.py tests/test_note_indexer.py tests/test_atlas_recall.py` from `apps/ai-service` | Passed |
| `PYTHONPATH=. uv run ruff check app/api/routes/atlas.py app/api/routes/notes.py tests/test_atlas_routes.py tests/test_notes_routes.py` from `apps/ai-service` | Passed after adding historical KP/note embedding backfill endpoints |
| `node --check scripts/atlas/reindex-embeddings.mjs` | Passed after adding the operator backfill command |
| `PYTHONPATH=. uv run pytest tests/test_agent_routes.py -q --no-cov` from `apps/ai-service` | Passed after adding the empty-scope Agent Atlas semantic recall contract test |
| `PYTHONPATH=. uv run ruff check tests/test_agent_routes.py` from `apps/ai-service` | Passed after adding the empty-scope Agent Atlas semantic recall contract test |
| `go test ./internal/knowledge/repository -run 'Test(KPRepo\|RelationRepo)CountEvidence' -count=1` from `apps/server-go` | Passed after adding graph evidence-count repository coverage |
| `go test ./internal/knowledge/...` from `apps/server-go` | Passed after adding graph evidence-count metadata, inspector, and graph filters |
| `go test ./...` from `apps/server-go` | Passed after adding graph evidence-count metadata, inspector, and graph filters |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after adding graph inspector and expanded filters |
| `pnpm --filter @aetherblog/admin build` | Passed after adding graph inspector and expanded filters; Vite emitted existing large-chunk warnings only |
| `rg -n "<select\|Loader2\|animate-spin\|P3-DEMO\|Phase 0 占位\|严禁" apps/admin/src/pages/atlas apps/admin/src/services/atlasService.ts apps/admin/src/components/layout/Sidebar.tsx` | No matches after adding graph inspector and expanded filters |
| `git diff --check` | Passed after graph inspector and expanded filter changes |
| Playwright smoke against Vite dev server for `/admin/atlas/graph` | Passed after adding graph inspector hit targets; mocked auth/site/settings/graph/event APIs, verified Graph Summary, clicked a KP to open the Knowledge Point inspector, then clicked a relation to open the Relation inspector |
| Playwright smoke against Vite dev server for `/admin/atlas/graph` viewport persistence | Passed after adding graph zoom/pan/minimap/layout persistence; mocked auth/site/settings/graph/event APIs, verified zoom controls and minimap, changed zoom, dragged the graph, saved layout to localStorage, reloaded, verified the saved layout restored, then reset layout |
| Playwright smoke against Vite dev server for `/admin/atlas/graph` filter presets | Passed after adding G1-04 saved filter presets; mocked auth/site/settings/graph/event APIs, saved a named keyword/type filter preset to scoped localStorage, changed filters, reapplied the preset, reloaded to verify persistence, then deleted the preset and confirmed storage cleared |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after making AetherHub send empty-scope semantic Atlas recall and count Note citations |
| `pnpm --filter @aetherblog/admin build` | Passed after making AetherHub send empty-scope semantic Atlas recall; Vite emitted existing large-chunk warnings only |
| `git diff --check` | Passed after empty-scope AetherHub Atlas recall changes |
| `go test ./internal/service -run 'TestNoteIndexerClient' -count=1` from `apps/server-go` | Passed after adding the internal ai-service note index client |
| `go test ./internal/service ./internal/knowledge/service -count=1` from `apps/server-go` | Passed after D2 note embedding worker and Atlas note recall changes |
| `go test ./...` from `apps/server-go` | Passed after D2 note embedding worker and migration 000074 changes |
| `go test ./internal/knowledge/service -run TestAtlasIndexerClient -v` from `apps/server-go` | Passed after adding the internal ai-service Atlas KP index client |
| `go test ./internal/knowledge/service -run 'TestAtlasSemanticSearchClient\|TestAtlasIndexerClient' -count=1` from `apps/server-go` | Passed after adding the internal ai-service Atlas semantic search client |
| `go test ./internal/knowledge/handler -run 'TestAtlasSearchKnowledgePoints' -count=1` from `apps/server-go` | Passed after adding semantic-first KP rerank merge coverage |
| `go test ./...` from `apps/server-go` | Passed after search-page semantic rerank wiring and server constructor updates |
| `PYTHONPATH=. uv run pytest tests/test_atlas_routes.py -q --no-cov -k semantic_search` from `apps/ai-service` | Passed after adding `/v1/atlas/search/semantic` route coverage |
| `PYTHONPATH=. uv run pytest tests/test_atlas_routes.py tests/test_atlas_recall.py -q --no-cov` from `apps/ai-service` | Passed after search-page semantic rerank backend wiring |
| `PYTHONPATH=. uv run pytest tests/test_atlas_routes.py tests/test_atlas_recall.py tests/test_agent_routes.py -q --no-cov` from `apps/ai-service` | Passed after confirming semantic search route did not regress Agent Atlas recall |
| `PYTHONPATH=. uv run ruff check app/api/routes/atlas.py app/services/atlas_recall.py tests/test_atlas_routes.py` from `apps/ai-service` | Passed after semantic search route and optional all-scope recall changes |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after adding the search-page semantic rerank toggle, response fields, and result chips |
| `pnpm --filter @aetherblog/admin build` | Passed after search-page semantic rerank UI; Vite emitted existing large-chunk warnings only |
| Playwright smoke against Vite dev server for `/admin/atlas/search` semantic rerank | Passed with mocked auth/search APIs; verified default `semantic=true`, URL `semantic=0` disables the toggle and sends `semantic=false`, source/score chips render, and degraded semantic status shows keyword fallback banner |
| `rg -n "<select\|Loader2\|animate-spin\|P3-DEMO\|Phase 0 占位\|严禁" apps/admin/src/pages/atlas apps/admin/src/services/atlasService.ts apps/admin/src/components/layout/Sidebar.tsx` | No matches after adding the search-page semantic rerank toggle |
| `git diff --check` | Passed after search-page semantic rerank changes |
| `go test ./internal/knowledge/service -run 'TestAtlasIndexerClient\|TestMarkdown\|TestPDF\|TestAnchoring' -v` from `apps/server-go` | Passed after wiring async KP indexing trigger support |
| `go test ./internal/knowledge/handler -run 'Test.*Atlas\|Test.*Suggestion\|Test.*Scope' -v` from `apps/server-go` | Passed after scheduling KP embedding from KP and suggestion handler paths |
| `go test ./...` from `apps/server-go` | Passed after Atlas semantic recall and async KP indexing changes |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after adding `semanticRecall` / `semanticLimit` to AetherHub Agent payload |
| `pnpm --filter @aetherblog/admin build` | Passed after adding `semanticRecall` / `semanticLimit`; Vite emitted existing large-chunk warnings only |
| `go run ./cmd/migrate -dir ./migrations up && go run ./cmd/migrate -dir ./migrations version` from `apps/server-go` with local dev `DATABASE_DSN` | Passed after adding migration `000074`; database migration version is `74`, dirty `false` |
| `python -m compileall apps/ai-service/app/api/routes/atlas.py apps/ai-service/app/api/routes/agent.py` | Passed |
| `node scripts/atlas/anchoring-recall.mjs --min-recall 0.9` | Passed; 23/23 Markdown version-migration anchorable cases recalled, 1/1 deliberate orphan matched |
| `go test ./internal/knowledge/service -run TestRelocateMarkdownRecallCorpus -v` from `apps/server-go` | Passed |
| `node scripts/atlas/pdf-anchoring-recall.mjs --min-recall 0.9` | Passed; 23/23 PDF text-layer version-migration anchorable cases recalled, 1/1 deliberate orphan matched |
| `node scripts/atlas/pdf-anchoring-recall.mjs --min-recall 0.9 --json` | Passed and emitted machine-readable PDF text-layer R1 metrics |
| `go test ./internal/knowledge/service -run 'TestRelocate(Markdown\|PDFTextLayer)RecallCorpus' -v` from `apps/server-go` | Passed both Markdown and PDF text-layer relocation recall corpora |
| `node --check scripts/atlas/pdf-real-corpus-gate.mjs` | Passed after adding the R1 file-level real-PDF corpus gate |
| `node scripts/atlas/pdf-real-corpus-gate.mjs --min-recall 0.9` | Passed; extracted 3 representative multi-page PDFs through `pypdf`, covered 9 original pages and 12 revision variants, recalled 45/45 anchorable checks, and matched 3/3 deliberate orphans |
| `go test ./internal/knowledge/service ./internal/knowledge/repository ./internal/server` from `apps/server-go` | Passed after PDF carrier text-layer persistence and AI extractor tests |
| `go test ./...` from `apps/server-go` | Passed after PDF carrier wiring |
| `PYTHONPATH=. .venv/bin/pytest tests/test_atlas_routes.py -q --no-cov` from `apps/ai-service` | Passed 4 Atlas route tests, including PDF text-layer page offsets and invalid base64 rejection |
| `node scripts/atlas/relation-health-gate.mjs` | Passed fixed corpus; density `2.00`, KP evidence `100%`, relation evidence/rationale `100%` |
| `node scripts/atlas/relation-health-gate.mjs --json` | Passed and emitted machine-readable R2 metrics |
| `node --check scripts/atlas/run-relation-health-live-gate.mjs` | Passed after adding the R2 live non-admin user dataset gate |
| `ATLAS_SMOKE_PASSWORD=... ATLAS_R2_LIVE_REPORT=output/playwright/atlas-r2-live-relation-health-report.json node scripts/atlas/run-relation-health-live-gate.mjs` | Passed on 2026-05-31T10:23:04Z; temporarily granted `AUTHOR` Atlas read/write, created non-admin author `15`, seeded 5 KPs / 10 relations through gateway API, and live graph health returned density `2.00`, KP evidence `100%`, relation evidence `100%`, orphan `0%` |
| `admin API role probe for AUTHOR content.atlas.* permissions after R2 live gate` | Passed; `atlasPerms=[]` confirms temporary Atlas permissions were restored |
| `node scripts/atlas/ai-quality-gate.mjs` | Passed fixed corpus; KP accept `62.50%`, relation accept `50.00%`, schema valid `100%`, grounded `100%` |
| `node scripts/atlas/ai-quality-gate.mjs --json` | Passed and emitted machine-readable R3 metrics |
| `node --check scripts/atlas/run-ai-quality-live-gate.mjs` | Passed after adding the R3 live AI-quality gate |
| `go run ./cmd/migrate -dir ./migrations up` from `apps/server-go` with local database DSN | Passed after adding migration `000072`; database migration version is `72`, dirty `false` |
| `ATLAS_SMOKE_PASSWORD=... ATLAS_R3_LIVE_REPORT=output/playwright/atlas-r3-live-ai-quality-report.json node scripts/atlas/run-ai-quality-live-gate.mjs` | Blocked as designed in the current local environment: Atlas task types and routing exist (`atlas_claims` / `atlas_relations` routed to `gpt-5-mini`), but both routes report no usable credential, so the gate refuses to count live AI quality evidence |
| `ATLAS_SMOKE_PASSWORD=... ATLAS_R3_MODEL_ID=gemini-3.1-flash-lite-preview ATLAS_R3_LIVE_REPORT=output/playwright/atlas-r3-live-ai-quality-report.json node scripts/atlas/run-ai-quality-live-gate.mjs` | Passed on 2026-05-31T10:52:25Z with a local AI-service decrypt-key override for the existing DB credential: generated 2 non-stub KP suggestions and 2 non-stub relation suggestions through gateway/server-go/ai-service; metrics were final `4`, KP accept `100%`, relation accept `50%`, schema valid `100%`, grounded `100%`, non-stub `100%`, token coverage `100%`; temporary `AUTHOR` Atlas permissions were restored |
| `node scripts/atlas/performance-budget-gate.mjs --allow-missing-runtime` | Passed build-stat budget; Atlas JS `213.3KiB`, largest JS asset `1.38MiB`; runtime metrics intentionally skipped |
| `node scripts/atlas/performance-budget-gate.mjs --allow-missing-runtime --json` | Passed and emitted machine-readable R4 build budget metrics |
| `node scripts/atlas/release-smoke-gate.mjs --print-template` | Passed template generation for the required 15-check R5 smoke matrix |
| `go run ./cmd/migrate -dir ./migrations version` from `apps/server-go` | Passed; database migration version `71`, dirty `false` before gateway smoke |
| `node --check scripts/atlas/run-release-smoke.mjs` | Passed after adding KP lifecycle browser checks to the authenticated gateway smoke runner |
| `node --check scripts/atlas/release-smoke-gate.mjs` | Passed after expanding the smoke matrix to current admin routes plus KP lifecycle checks |
| `ATLAS_SMOKE_PASSWORD=... ATLAS_SMOKE_REPORT=output/playwright/atlas-release-smoke-report.json node scripts/atlas/run-release-smoke.mjs` | Passed after adding KP lifecycle browser checks; seeded note `15`, Markdown carrier `45`, annotation `15`, KP `40`, related KP `41`, lifecycle KP `42`, relation `12`, suggestion `18`, PDF media `33`, PDF carrier `46`; smoke passed 15/15 with LCP `956ms` and graph FPS `59.7` |
| `node scripts/atlas/release-smoke-gate.mjs --input output/playwright/atlas-release-smoke-report.json` | Passed; required `15`, passed checks `15`, blocked `0`, failed `0`, missing `0` |
| `node scripts/atlas/performance-budget-gate.mjs --lcp-ms 956 --graph-fps 59.7` | Passed current R4 runtime/build budget; Atlas JS `242.9KiB`, largest JS asset `1.38MiB`, LCP `956ms`, graph FPS `59.7` |
| `node --check scripts/atlas/run-multiuser-smoke.mjs && node --check scripts/atlas/multiuser-smoke-gate.mjs` | Passed after adding the non-admin/admin gateway smoke runner and verifier |
| `go test ./internal/knowledge/service ./internal/knowledge/handler` from `apps/server-go` | Passed after adding scoped Markdown source API and service coverage for owner/admin access |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after changing Markdown Reader to use the Atlas scoped Markdown source API |
| `ATLAS_SMOKE_PASSWORD=... ATLAS_MULTIUSER_REPORT=output/playwright/atlas-multiuser-smoke-report.json node scripts/atlas/run-multiuser-smoke.mjs` | Passed on 2026-05-31T10:05:25Z; temporarily granted `AUTHOR` Atlas read/write, created non-admin authors `13`/`14`, seeded Reader notes `16`/`17` and KP `43`/`44`, opened both non-admin Reader routes, verified cross-user Reader source `403`, passed 17/17 admin/non-admin scope checks, and restored role permissions in `finally` |
| `node scripts/atlas/multiuser-smoke-gate.mjs --input output/playwright/atlas-multiuser-smoke-report.json` | Passed; required `17`, passed checks `17`, blocked `0`, failed `0`, missing `0` |
| `admin API role probe for AUTHOR content.atlas.* permissions` | Passed after multi-user smoke; `atlasPerms=[]` confirms temporary Atlas permissions were restored |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after changing the admin API client to treat `403` as authorization failure rather than token-expiry logout; passed again after the R5 lifecycle smoke/gate update |
| `go test ./internal/knowledge/handler -run 'TestSuggestionHandlerCallAtlasAI\|TestSuggestionHandlerMountsCarrierSuggestionRoute\|TestNormalizeCarrierSuggestionRequestDefaultsAndCaps\|TestMarkdownNoteIDFromCarrierSource\|TestAnnotationTextForSuggestion' -count=1` from `apps/server-go` | Passed after adding carrier-level AI suggestion route coverage |
| `PYTHONPATH=. uv run pytest tests/test_atlas_routes.py --no-cov` from `apps/ai-service` | Passed after adding carrier extraction cost preview, `maxCostUsd` LlmRouter propagation, and budget-exceeded non-fallback coverage |
| `go test ./internal/knowledge/handler -run 'TestSuggestionHandlerCallAtlasAI\|TestSuggestionHandlerMountsCarrierSuggestionRoute\|TestSuggestionHandlerMountsCarrierSuggestionPreviewRoute\|TestCarrierSuggestionAIPayloadIncludesCostBudget\|TestNormalizeCarrierSuggestionRequestDefaultsAndCaps\|TestMarkdownNoteIDFromCarrierSource\|TestAnnotationTextForSuggestion' -count=1` from `apps/server-go` | Passed after adding carrier suggestion cost preview route and budget payload coverage |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after adding Reader carrier suggestion preflight cost preview and threshold guard |
| `go test ./...` from `apps/server-go` | Passed after adding carrier suggestion cost preview and budget guard |
| `pnpm --filter @aetherblog/admin build` | Passed after adding Reader carrier suggestion preflight cost preview and threshold guard; Vite emitted existing large-chunk warnings only |
| Playwright smoke against Vite dev server for Reader carrier-level AI budget preflight | Passed with mocked auth/Atlas APIs; Markdown Reader clicked `全文 AI 建议` and verified preview then generate both sent `maxCandidates: 8` and `maxCostUsd: 0.05`; PDF Reader preview returned `budgetExceeded: true` and generation was blocked |
| `go test ./internal/knowledge/...` from `apps/server-go` | Passed after adding P2-02 carrier-level AI suggestion generation |
| `go test ./...` from `apps/server-go` | Passed after adding P2-02 carrier-level AI suggestion generation |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after adding Markdown/PDF Reader `全文 AI 建议` controls and service client method |
| `pnpm --filter @aetherblog/admin build` | Passed after adding Markdown/PDF Reader `全文 AI 建议` controls; Vite emitted existing large-chunk warnings only |
| `rg -n "<select\|Loader2\|animate-spin\|P3-DEMO\|Phase 0 占位\|严禁" apps/admin/src/pages/atlas apps/admin/src/services/atlasService.ts apps/admin/src/components/layout/Sidebar.tsx` | No matches after adding P2-02 carrier-level AI suggestion controls |
| Playwright smoke against Vite dev server for Reader carrier-level AI suggestions | Passed with mocked auth/Atlas APIs; opened Markdown Reader and PDF Reader, clicked `全文 AI 建议`, and verified both pages POST `maxCandidates: 8` to `/atlas/carriers/:id/suggestions` |
| `git diff --check` | Passed |
| Browser smoke | In-app Browser was unavailable in this environment; Playwright could load `/admin/atlas` through the Vite dev server and reached the expected login screen without a Vite overlay |
| `go test ./internal/knowledge/service -run 'TestBlogPost\|TestAtlasServiceAttachBlogPosts' -count=1` from `apps/server-go` | Passed after adding blog post carrier service source scope, text normalization, and AtlasService attachment coverage |
| `go test ./internal/knowledge/handler -run 'TestBlogPostIDFromCarrierSource\|TestCarrierHandlerMountsPostCarrierRoute\|TestSuggestionHandlerMountsCarrierSuggestionRoute' -count=1` from `apps/server-go` | Passed after adding `/atlas/carriers/post` route and `posts://{id}` source parsing coverage |
| `go test ./internal/knowledge/...` from `apps/server-go` | Passed after adding blog post carrier support |
| `go test ./...` from `apps/server-go` | Passed after wiring blog post carriers into server setup |
| `pnpm --filter @aetherblog/admin typecheck` | Passed after adding AI Writing workspace Atlas suggestion action and `blog_post` shared type |
| `pnpm --filter @aetherblog/admin build` | Passed after adding AI Writing workspace Atlas suggestion action; Vite emitted existing large-chunk warnings only |
| `rg -n "<select\|Loader2\|animate-spin\|P3-DEMO\|Phase 0 占位\|严禁" apps/admin/src/pages/atlas apps/admin/src/pages/posts/AiWritingWorkspacePage.tsx apps/admin/src/services/atlasService.ts apps/admin/src/components/layout/Sidebar.tsx` | No matches after adding the writing-workspace Atlas action |
| Playwright smoke against Vite dev server for AI Writing Atlas post suggestions | Passed with mocked auth/Atlas APIs; opened `/admin/posts/ai-writing/123`, clicked `生成 Atlas 建议`, verified `POST /atlas/carriers/post` sent `{postId:123}`, then preview and generate both sent `maxCandidates: 8` and `maxCostUsd: 0.05` |
| Playwright desktop smoke against Vite dev server for AI Writing Atlas references | Passed with mocked auth/Atlas APIs; opened `/admin/posts/ai-writing/123`, filled a title, clicked `Atlas 参考`, verified `GET /atlas/search` sent `scope=mine&semantic=true&limit=6`, rendered a related KP, clicked `插入引用`, and verified the editor body contained `Atlas KP #42` |
| Playwright mobile smoke against Vite dev server for AI Writing Atlas references | Passed at `390x844`; opened the same route, clicked the mobile `Atlas 参考` action, rendered a related KP in the bottom sheet, inserted the reference, and verified the editor body contained `Atlas KP #43` |
| `rg -n "blog_post" apps/server-go/migrations/000075_atlas_blog_post_carrier_type.up.sql apps/server-go/migrations/000075_atlas_blog_post_carrier_type.down.sql apps/server-go/internal/knowledge/model/carrier.go` | Passed; confirms the schema patch allows `blog_post` carrier rows and the Go model comment lists the same carrier type |
| `go test ./internal/knowledge/service -run 'TestBlogPost\|TestAtlasServiceAttachBlogPosts' -count=1` from `apps/server-go` | Passed after adding migration `000075` to close the real DB CHECK constraint for the existing blog-post carrier service path |
| `go test ./internal/knowledge/handler -run 'TestBlogPostIDFromCarrierSource\|TestCarrierHandlerMountsPostCarrierRoute\|TestSuggestionHandlerMountsCarrierSuggestionRoute' -count=1` from `apps/server-go` | Passed after adding migration `000075`; route/source parsing tests continue to cover the blog-post carrier path |
| `go test ./internal/knowledge/service -run 'TestWebClip\|TestAtlasServiceAttachWebClips' -count=1` from `apps/server-go` | Passed after adding Web clip URL normalization, rootText construction, and AtlasService attachment coverage |
| `go test ./internal/knowledge/handler -run 'TestCarrierHandlerMountsWebCarrierRoute' -count=1` from `apps/server-go` | Passed after mounting `POST /atlas/carriers/web` |
| `node --check scripts/atlas/release-smoke-gate.mjs && node --check scripts/atlas/run-release-smoke.mjs && node scripts/atlas/release-smoke-gate.mjs --print-template \| rg 'atlas-reader-web'` | Passed after adding Web Reader to the release smoke matrix |
| Playwright smoke against Vite dev server for `/admin/atlas` -> `/admin/atlas/reader/web/:carrierId` | Passed with mocked auth/Atlas APIs: saved a Web snapshot through the dashboard modal, verified the `POST /atlas/carriers/web` payload, followed the redirect to Web Reader, and rendered the saved text-layer anchor |

## Not Yet Passed

None for the scoped R1/R2/R3/R4/R5 gate baseline in this PR. Production deployment reruns, production default Atlas routing credential setup, production execution of the KP/note embedding backfill command, broader prompt/model A/B comparisons, real user telemetry, full GraphRAG/community/global query, public knowledge-map surfaces, browser/readability automatic capture, and multimodal expansion beyond the current PDF/blog-post/Web Reader baselines remain tracked as follow-up roadmap or release evidence rather than current PR gate blockers.
