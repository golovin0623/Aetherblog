┌─────────────────────────────────────────────────────────────────┐
│                 GitHub Actions 快速参考                         │
└─────────────────────────────────────────────────────────────────┘

📋 工作流列表
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. docker-build-push.yml  - 自动构建 Docker 镜像
2. ci-cd.yml              - 完整 CI/CD (测试+构建+部署)
3. quick-build.yml        - 快速构建 (跳过测试)

🚀 常用命令
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 触发自动构建
git push origin main

# 创建版本发布
git tag v1.0.0
git push origin v1.0.0

# 查看工作流状态
gh run list
gh run watch

# 手动触发工作流
gh workflow run "Quick Docker Build" \
  -f service=all \
  -f version=v1.0.0

🔧 首次设置
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 方式 1: 使用脚本 (推荐)
./.github/setup-secrets.sh

# 方式 2: 手动设置
gh secret set DOCKER_USERNAME
gh secret set DOCKER_PASSWORD

# 方式 3: 在 GitHub 网页设置
Settings → Secrets and variables → Actions → New secret

📦 镜像命名
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
golovin0623/aetherblog-backend:v1.0.0
golovin0623/aetherblog-backend:latest
golovin0623/aetherblog-blog:v1.0.0
golovin0623/aetherblog-blog:latest
golovin0623/aetherblog-admin:v1.0.0
golovin0623/aetherblog-admin:latest

🔄 部署流程
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 推送代码 → 2. 自动构建 → 3. 推送镜像 → 4. 服务器拉取

# 在服务器上更新
ssh user@server
cd /opt/aetherblog
docker-compose -f docker-compose.prod.yml pull
docker-compose -f docker-compose.prod.yml up -d

💡 提示
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
• 构建时间: 5-10 分钟 (首次), 2-5 分钟 (有缓存)
• 并行构建: 3 个服务同时构建
• 缓存策略: 自动使用 Docker 层缓存
• 多平台: 支持 amd64 和 arm64

📚 详细文档
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
.github/CICD_GUIDE.md       - 完整配置指南
.github/workflows/README.md - 工作流说明
